/**
 * _strlen - returns length of a string
 * @s: string to measure
 *
 * Return: length
 */
int _strlen(char *s)
{
	int length;

	for (length = 0; *s != '\0'; length++, s++)
	{
	}

	return (length);
}
